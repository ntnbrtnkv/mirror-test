stages:
  - test
  - deploy
  
Test:
  stage: test
  script:
    - cat README.md
    - echo "$CI_JOB_ID" > _job_id

Deploy:
  stage: deploy
  variables:
    REF_NAME: $CI_COMMIT_REF_NAME
  trigger: 
    project: a.bortnikov/mirror-test-ci
    strategy: depend
