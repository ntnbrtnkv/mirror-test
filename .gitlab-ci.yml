stages:
  - test
  
Test:
  stage: test
  script:
    - cat README.md
    - echo "$CI_JOB_ID" > _job_id
  artifacts:
    paths:
      - README.md
    expire_in: 2 hrs

staging:
  variables:
    MY_JOB_ID: 
  trigger: a.bortnikov/mirror-test-ci
